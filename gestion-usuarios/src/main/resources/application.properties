spring.application.name=gestion-usuarios

# Database Configuration - SET THESE VIA ENVIRONMENT VARIABLES
spring.datasource.url=${DB_URL:jdbc:oracle:thin:@rpwbb3wdet77677f_high}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.oracle.wallet.password=${WALLET_PASSWORD}

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect